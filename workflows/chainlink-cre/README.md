# Chainlink CRE Workflow

The confidential workflow will combine a public intent and deterministic
simulation summary with private risk thresholds. It returns a schema-validated
`ALLOW`, `BLOCK`, or `ESCALATE` result without exposing the private thresholds.
